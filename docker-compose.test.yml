# Local SFTP server for integration tests (atmoz/sftp).
#   docker compose -f docker-compose.test.yml up -d
#   TEST_SFTP=1 npm test
services:
  sftp:
    image: atmoz/sftp
    ports:
      - "2222:22"
    command: gateway:gatewaypass:::inbound
